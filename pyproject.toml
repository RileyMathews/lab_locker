[tool.poetry]
name = "lab-locker"
version = "0.1.0"
description = ""
authors = ["Riley Mathews <rileymathews80@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"
django = "^5.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
